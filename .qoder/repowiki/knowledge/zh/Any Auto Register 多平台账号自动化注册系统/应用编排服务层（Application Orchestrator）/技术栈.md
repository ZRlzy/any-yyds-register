SQLModel (Session) 直接操作 `core.db` 中的 TaskModel/TaskEventModel/AccountModel；并发控制使用 `concurrent.futures.ThreadPoolExecutor` + `wait(FIRST_COMPLETED)`；任务锁通过模块级 `_task_locks` 字典 + `threading.Lock` 实现 per-task 互斥。