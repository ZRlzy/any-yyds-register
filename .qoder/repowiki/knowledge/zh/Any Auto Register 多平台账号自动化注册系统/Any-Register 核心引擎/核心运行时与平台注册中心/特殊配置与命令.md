运行前需确保 `platforms/` 目录下各平台包包含 `plugin` 模块并被 `registry.load_all()` 自动扫描；Playwright 模式需预先安装 Chromium（`playwright install chromium`）；本地 solver 类型的 captcha provider 会在首次使用时通过 `services.solver_manager.start()` 启动。