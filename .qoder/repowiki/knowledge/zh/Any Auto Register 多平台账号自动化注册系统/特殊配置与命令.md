使用 `docker compose up` 一键启动完整环境（含 Chromium/XvFB/noVNC/Solver）；本地开发可通过 `python main.py` 启动 API 服务，或传入 `--solver` 参数以 PyInstaller 模式单独运行 Turnstile Solver 子进程；Docker 镜像通过 `APP_VERSION` 构建参数注入版本号，并将前端构建产物复制到 `/app/static`。