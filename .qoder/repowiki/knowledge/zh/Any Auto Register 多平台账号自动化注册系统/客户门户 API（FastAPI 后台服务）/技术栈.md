FastAPI + Uvicorn 作为 Web 框架；SQLModel（基于 SQLAlchemy）进行 ORM 建模；SQLite（默认文件库 `customer_portal.db`，可通过 `PORTAL_DATABASE_URL` 切换）；自定义 HS256 JWT（使用 `hmac` + `base64url`，无第三方 JWT 库）；密码哈希使用 `hashlib.pbkdf2_hmac` 20 万次迭代；SSE 流式事件通过 `StreamingResponse` 实现。